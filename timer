#!/bin/bash

#  Decription
#  ===========
#  Simple timer with sound and bubble notification. To remind you to turn stove off :). Replace pc_up.wav to any available audiofile. 

#  Requirements
#  ============
#  sudo apt-get install vlc

#  Usage
#  =====
#  timer 6 # i.e. sleep 6 minutes

sleep $(expr $1 \* 60) && notify-send "Timer done" && vlc -q --no-repeat --play-and-exit -I dummy /home/graf/documents/sounds/quake/pc_up.wav &

